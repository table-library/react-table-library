import { Meta } from '@storybook/addon-docs';

<Meta title="Types/Theme" />

# Theme

```javascript
export type Theme = {
  Table?: string,
  Header?: string,
  Footer?: string,
  Body?: string,
  BaseRow?: string,
  HeaderRow?: string,
  FooterRow?: string,
  Row?: string,
  BaseCell?: string,
  HeaderCell?: string,
  FooterCell?: string,
  Cell?: string,
};
```

Access all feature related types:

```javascript
import * as TYPES from '@overmap-ai/react-table-library/types/theme';
```

Access **all** types:

```javascript
import * as TYPES from '@overmap-ai/react-table-library/types';
```
