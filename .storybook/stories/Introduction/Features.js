import * as React from 'react';
import { storiesOf } from '@storybook/react';

storiesOf('00. Intro/ 02. Features', module).add('default', () => {
  return (
    <p>
      Features .... - [ ] composition over configuration - [ ] highly
      customisable (theming, composing) - [ ] advanced feature rich
      (e.g. tree, load more) - [ ] maximum user control - [ ]
      server-side first-class citizen
    </p>
  );
});
