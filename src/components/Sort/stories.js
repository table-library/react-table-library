import React from 'react';
import { storiesOf } from '@storybook/react';

storiesOf('02. Table with Sort', module)
  // .addParameters({ component: Table })
  .add('01. with ascending/descending', () => {
    return <div>WIP</div>;
  });
